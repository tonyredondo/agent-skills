name: Code Diff Walkthrough Tests

on:
  push:
    paths:
      - "code-diff-walkthrough/**"
      - ".github/workflows/code-diff-walkthrough-tests.yml"
  pull_request:
    paths:
      - "code-diff-walkthrough/**"
      - ".github/workflows/code-diff-walkthrough-tests.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unittest:
    name: Unittest (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    defaults:
      run:
        working-directory: code-diff-walkthrough
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run all tests
        env:
          PYTHONUNBUFFERED: "1"
        run: |
          python -m unittest discover -s tests -p "test_*.py"
